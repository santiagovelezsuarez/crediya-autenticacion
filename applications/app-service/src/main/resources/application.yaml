server:
  port: 8080
  servlet:
    encoding:
      enabled: true
      charset: "UTF-8"
      force: true
spring:
  application:
    name: "crediya-autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY:8d0ec032515e2e10331c05c99086f2a117ab8c23cd19ec71209cf77354c38870}
    expiration-time: ${JWT_EXPIRATION:86400}
    issuer: ${JWT_ISSUER:crediya-autenticacion}

cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

adapters:
  r2dbc:
    host: "192.168.1.100"
    port: 5432
    database: "crediya_autenticacion"
    schema: "public"
    username: "santi"
    password: "1926"

validation:
  salary:
    max: 15000000